<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
	PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.project.mingle.mapper.CodyMapper">
	<select id="?" resultType="com.project.mingle.vo.CodyTestVO">
		select , request_name, request_comment, user_id, request_writedate from request where no=${param1}
	</select>


</mapper>